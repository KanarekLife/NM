function [A,b,M,bm,x,err_norm,time,iterations,index_number] = solve_Jacobi(N)
    index_number = 193044;
    L1 = mod(index_number, 10);
    [A,b] = generate_matrix(N, L1);

    x = ones(N, 1);

    M = -diag(A)' * (tril(A) + triu(A));
    
end